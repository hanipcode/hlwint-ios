import { put, actionChannel, call, fork } from 'redux-saga/effects';
import * as service from '../helpers/api';

// function listenSendGift() {
//   const giftChannel = yield;
// }

// export default function* watcher() {
//   while (true) {}
// }
