import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  logo: {
    height: 100,
    resizeMode: 'contain',
    marginBottom: 70,
  },
  loginButton: {
    height: 40,
    alignItems: 'center',
    justifyContent: 'center',
    width: 300,
  },
});
